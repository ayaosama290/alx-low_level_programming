#include "main.h"
/**
 * add - adds two numbers
 *
 * Description: a function that adds two numbers abd return result
 * @a: the first number
 * @b: the second number
 *
 * Return: 0 if success
 */
int add(int a, int b)
{
	res = a + b;
	_putchar(res + 48);
	return (0);
}
